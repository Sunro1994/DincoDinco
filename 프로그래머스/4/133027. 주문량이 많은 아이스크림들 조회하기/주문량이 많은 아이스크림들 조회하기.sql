-- 코드를 입력하세요
SELECT A.FLAVOR
FROM(
SELECT 
FLAVOR, 
SUM(TOTAL_ORDER) AS TOTAL1
FROM FIRST_HALF
GROUP BY FLAVOR
) A
JOIN 
(
SELECT 
FLAVOR, 
SUM(TOTAL_ORDER) AS TOTAL2
FROM JULY
GROUP BY FLAVOR
) B 
ON A.FLAVOR = B.FLAVOR
ORDER BY (TOTAL1 + TOTAL2) DESC
LIMIT 3